
---

## 🤖 5️⃣ GitHub Auto-Deploy (OPTIONAL but PRO)

### `.github/workflows/deploy.yml`

```yaml
name: Deploy Bot

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - name: Install deps
        run: pip install -r requirements.txt

